---
phase: 01-code-rebuild-and-stats-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - package.json
autonomous: true
requirements:
  - CONF-01
  - CONF-03

must_haves:
  truths:
    - "vercel.json declares Fluid Compute and sets maxDuration to 60 seconds"
    - "package.json pins Node.js to 22.x via the engines field"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel platform configuration with Fluid Compute and correct timeout"
      contains: "fluid"
    - path: "package.json"
      provides: "Node.js version pin"
      contains: "engines"
  key_links:
    - from: "vercel.json"
      to: "api/resolve.js execFile timeout"
      via: "maxDuration: 60 creates a 60s function budget; execFile in plan 02 must be set to 57s"
      pattern: "maxDuration.*60"
---

<objective>
Update the two platform configuration files to match the correct Vercel Fluid Compute settings and pin the Node.js version.

Purpose: The current vercel.json has maxDuration: 20 (too short for slow sites, inconsistent with the code) and is missing the "fluid": true declaration. package.json has no engines field, so Vercel picks whatever Node.js default it feels like. Both files need one-line fixes.

Output: vercel.json with "fluid": true and maxDuration: 60; package.json with "engines": { "node": "22.x" }.
</objective>

<execution_context>
@C:/Users/enggy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/enggy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update vercel.json — add Fluid Compute and correct maxDuration</name>
  <files>vercel.json</files>
  <action>
Rewrite vercel.json to:
1. Add "fluid": true at the top level — this explicitly enables Fluid Compute regardless of project creation date (safe to add even if already enabled)
2. Change maxDuration from 20 to 60 in the functions block
3. Keep the existing "version": 2 and "rewrites" block unchanged

Target file content:
```json
{
  "version": 2,
  "fluid": true,
  "functions": {
    "api/*.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/resolve" }
  ]
}
```

Note: 60s is a conservative choice. Fluid Compute on Hobby tier allows up to 300s. If yt-dlp times out at exactly 60s in production, this value can be raised. The execFile timeout in api/resolve.js (handled in plan 02) will be set to 57s — 3 seconds less than maxDuration to leave room for response overhead.
  </action>
  <verify>node -e "const v = require('./vercel.json'); if (!v.fluid) throw new Error('fluid missing'); if (v.functions['api/*.js'].maxDuration !== 60) throw new Error('maxDuration wrong'); console.log('OK')"</verify>
  <done>vercel.json has fluid: true and maxDuration: 60; rewrites block is unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json — pin Node.js 22.x via engines field</name>
  <files>package.json</files>
  <action>
Add an "engines" field to package.json with "node": "22.x". This overrides whatever Vercel's dashboard default is (currently 24.x as of 2026) and ensures 22.x LTS is used consistently.

Target file content:
```json
{
  "name": "worker-c",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {}
}
```

Do not add any other fields. The file is intentionally minimal — no scripts, no devDependencies.
  </action>
  <verify>node -e "const p = require('./package.json'); if (!p.engines || p.engines.node !== '22.x') throw new Error('engines missing or wrong'); console.log('OK')"</verify>
  <done>package.json has engines.node set to "22.x"; all other fields unchanged</done>
</task>

</tasks>

<verification>
Both files parse as valid JSON and contain the required fields:
- `node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))"` — no parse error
- `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` — no parse error
- vercel.json: fluid === true, maxDuration === 60
- package.json: engines.node === "22.x"
</verification>

<success_criteria>
- vercel.json declares fluid compute and sets maxDuration to 60 (CONF-01)
- package.json pins Node.js 22.x via engines field (CONF-03)
- Both files are valid JSON with no syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-rebuild-and-stats-hardening/01-01-SUMMARY.md` following the summary template.
</output>
