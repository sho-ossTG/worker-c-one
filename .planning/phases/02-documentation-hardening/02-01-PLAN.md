---
phase: 02-documentation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLONE.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "CLONE.md exists at repo root as a numbered checklist"
    - "A first-time operator can follow CLONE.md from clone to verified-working worker without reading any other file"
    - "CLONE.md includes an env var table inline after the Set env vars step with exact names, example values, and what breaks if missing"
    - "CLONE.md includes a Common Mistakes section covering WORKER_SECRET not set, WORKER_ID collision, and Fluid Compute disabled"
    - "CLONE.md includes a one-paragraph system architecture summary (Worker C -> Server B -> Server A)"
    - "CLONE.md ends with a compact endpoint reference table and an Adding a second worker note"
    - "WORKER_SECRET gets double-emphasis: blockquote warning inline in env var step AND entry in Common Mistakes"
  artifacts:
    - path: "CLONE.md"
      provides: "Operator deployment checklist from clone to verified-working"
      min_lines: 80
  key_links:
    - from: "CLONE.md env var table"
      to: "api/resolve.js env var defaults"
      via: "exact variable names WORKER_ID and WORKER_SECRET"
      pattern: "WORKER_ID.*worker-c1"
    - from: "CLONE.md verification step"
      to: "status page IDLE/WORKING states"
      via: "explicit browser navigation instruction"
      pattern: "IDLE.*WORKING"
---

<objective>
Create CLONE.md at repo root — a numbered deployment checklist that takes a first-time operator from zero (no repo, no Vercel account knowledge) to a verified-working Worker C instance.

Purpose: Any operator who clones this repo and follows CLONE.md must end up with a correctly configured, registered worker — with zero misconfiguration possible if they complete the checklist.
Output: CLONE.md at repo root (~80-120 lines of Markdown)
</objective>

<execution_context>
@C:/Users/enggy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/enggy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-documentation-hardening/02-CONTEXT.md

<interfaces>
<!-- Authoritative values extracted from Phase 1 source files. Use these exactly — do not guess. -->

From api/resolve.js:
  WORKER_ID default:     process.env.WORKER_ID || "worker"
  WORKER_SECRET default: process.env.WORKER_SECRET || ""  (empty = auth disabled)
  execFile timeout:      57000ms

From vercel.json:
  "fluid": true
  "maxDuration": 60
  All paths rewrite to api/resolve.js

Endpoints:
  GET /               — HTML status page (public, no auth)
  GET /health         — JSON health check (public, no auth)
  GET /resolve?url=X  — Resolve video URL (requires Authorization: Bearer WORKER_SECRET)
  GET /?url=X         — Also resolves (vercel.json rewrites all paths to api/resolve.js)

Status page headline states:
  IDLE      — no resolves yet (grey dot)
  WORKING   — last resolve succeeded (green, animated dot)
  DEGRADED  — last resolve failed (red dot)
  BINARY ERROR — yt-dlp binary missing or broken (orange dot)

Health response schema:
  { "status": "ok", "worker_id": "worker-c1", "yt_dlp_version": "...", "error": null, "region": "iad1", "timestamp": "..." }
  HTTP 200 = healthy, HTTP 503 = degraded

Binary: bin/dlp-jipi (pre-compiled, already present in repo — no build step needed)

WORKER_SECRET callout (use verbatim from CONTEXT.md):
  > **⚠ Required for security:** Without this, anyone who knows your worker URL can use it. Set this before sharing the URL with Server B.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CLONE.md</name>
  <files>CLONE.md</files>
  <action>
Create CLONE.md at the repo root. Follow all locked decisions from 02-CONTEXT.md exactly. Do not add sections, steps, or content not specified here.

**Document structure — in this exact order:**

1. Title: "# Deploying a Worker C Instance"

2. Architecture summary — one paragraph: Worker C (this repo) receives a video URL, runs yt-dlp, and returns the direct media stream URL. Server B is the brain: it load-balances requests across workers and caches results. Server A is the Stremio-facing API that forwards requests to B. As an operator, you only need to deploy Worker C — this guide covers exactly that.

3. Numbered main flow — exactly four numbered steps:

   **Step 1: Clone the repo**
   - git clone command with placeholder URL
   - cd into the directory

   **Step 2: Create a Vercel project**
   - Go to vercel.com, click "Add New Project"
   - Import the Git repo
   - Do NOT click Deploy yet — env vars must be set first (note this explicitly)
   - Sub-step: After the project is created, go to Settings → Functions and confirm Fluid Compute is enabled. This lets yt-dlp run up to 60 seconds. Without it, requests silently time out at 10 seconds.

   **Step 3: Set environment variables**
   - Instruction: Go to Settings → Environment Variables
   - Env var table immediately after that instruction (inline, not at the end):

     | Variable | Example value | Required | What breaks if missing |
     |---|---|---|---|
     | `WORKER_ID` | `worker-c1` | Yes | All responses report `worker_id: "worker"` — Server B cannot distinguish workers |
     | `WORKER_SECRET` | `abc123-long-random-string` | Recommended | `/resolve` endpoint accepts requests without authentication |

   - After table: the WORKER_SECRET blockquote callout verbatim:
     > **⚠ Required for security:** Without this, anyone who knows your worker URL can use it. Set this before sharing the URL with Server B.

   - Then: "Click **Save** after adding each variable. Then click **Deploy** and wait for the deployment to complete."

   **Step 4: Verify**
   - Exact instruction: "Open `https://{your-project}.vercel.app` in your browser."
   - Exactly what to see: "The page header shows **IDLE** or **WORKING**. IDLE means the worker is online but has not resolved any URLs yet. WORKING means it has resolved at least one URL successfully."
   - If broken: "If the header shows BINARY ERROR or DEGRADED, check the error text on the status page."

4. Endpoint reference table — after the checklist:

   ## Endpoints

   | Endpoint | Auth | Returns |
   |---|---|---|
   | `GET /` | None | HTML status page |
   | `GET /health` | None | JSON `{"status":"ok","worker_id":"...","yt_dlp_version":"..."}` |
   | `GET /resolve?url=X` | `Authorization: Bearer WORKER_SECRET` | JSON `{"url":"...","worker_id":"..."}` on success |

5. Common mistakes section:

   ## Common Mistakes

   Three entries, each with bold Mistake title, then Symptom, then Fix:

   **Mistake: WORKER_SECRET not set**
   - **Symptom:** The status page shows "Auth: ⚠ disabled" in yellow. Anyone with your worker URL can call `/resolve` without credentials.
   - **Fix:** Add `WORKER_SECRET` to Settings → Environment Variables and redeploy.

   **Mistake: WORKER_ID not set or duplicated across workers**
   - **Symptom:** Multiple workers all respond with the same `worker_id` (e.g., `"worker"` or `"worker-c1"`). Server B cannot distinguish which worker handled or failed a request.
   - **Fix:** Each deployed worker must have a unique `WORKER_ID` (e.g., `worker-c1`, `worker-c2`). Update in Settings → Environment Variables and redeploy.

   **Mistake: Fluid Compute not enabled**
   - **Symptom:** yt-dlp calls return a timeout error after about 10 seconds, even though `vercel.json` sets `maxDuration: 60`.
   - **Fix:** Go to the Vercel project dashboard → Settings → Functions → enable Fluid Compute. The setting in `vercel.json` alone is not enough; the dashboard toggle must also be on.

6. Adding a second worker — brief closing section:

   ## Adding a Second Worker

   Same steps above. Use a new Vercel project with a different project name. Set `WORKER_ID=worker-c2` (or any unique name). Use the same `WORKER_SECRET` as all other workers. Add the new deployment URL to Server B's `WORKER_URLS` environment variable. No code changes required anywhere.

**Formatting rules:**
- Critical callouts: `> **⚠ Label:** explanation`
- Vercel dashboard paths: **Settings → Environment Variables** (bold)
- Every step has a concrete, observable outcome — no vague instructions
- Do NOT add steps to the main flow beyond the four specified stages
  </action>
  <verify>
node -e "
const fs = require('fs');
const md = fs.readFileSync('CLONE.md', 'utf8');
const required = [
  'WORKER_ID',
  'WORKER_SECRET',
  'Common Mistakes',
  'Fluid Compute',
  'IDLE',
  'WORKING',
  'Adding a Second Worker',
  '/resolve',
  '/health',
  '**⚠',
  'Server B',
  'worker-c1'
];
const missing = required.filter(s => !md.includes(s));
if (missing.length > 0) { console.error('MISSING:', missing.join(', ')); process.exit(1); }
const lines = md.split('\n').length;
if (lines < 80) { console.error('Too short:', lines, 'lines (need 80+)'); process.exit(1); }
console.log('CLONE.md OK —', lines, 'lines, all required sections present');
"
  </verify>
  <done>CLONE.md exists at repo root, passes the node verification script (all required sections present, 80+ lines)</done>
</task>

</tasks>

<verification>
Run the automated verify command from Task 1. CLONE.md must exist at repo root with all required sections.
</verification>

<success_criteria>
- CLONE.md exists at repo root
- Contains four-stage numbered checklist (clone, deploy, env vars, verify)
- Env var table is inline in the Set env vars step
- WORKER_SECRET has blockquote warning in env var step AND entry in Common Mistakes
- Three common mistakes documented with Symptom + Fix format
- Architecture paragraph present
- Endpoint reference table present
- Adding a second worker section present
- 80+ lines
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-hardening/02-01-SUMMARY.md`
</output>
